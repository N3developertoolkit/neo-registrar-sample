{
  "name": "register",
  "type": "neo-contract",
  "request": "launch",
  // "preLaunchTask": "build",
  "program": "${workspaceFolder}/src/bin/sc/registrar.nef",
  "neo-express": "${workspaceFolder}/default.neo-express",
  "invocation": {
    "operation": "register",
    "args": [
      "sample.domain",
      "@bob"
    ]
  },
  "return-types": [
    "bool"
  ]
}